ONE difference remains: a 0.0018" entry offset on the two SPLINE contours (4 and 5), which
are each cut twice - shallow by T5, through by T3 - so the same artifact shows on ten passes
(harness reports the first, pass 5: ref (3.6712, 72.2940) vs ours (3.6720, 72.2924); a full
per-pass audit shows every other pass in the program matches within 0.001"). The project
file settles WHY, and the answer is that this is a tessellation artifact, not information we
are missing.

From source.crv3d (see CRV3D.md):

  - The true source curve is a cubic bezier; its exact endpoint tangent is 115.372 deg
    (anchor (3.50109, 72.20942), first control (3.31824, 72.59499)).
  - Vectric's own stored T5 toolpath is 65 segments; its FIRST CHORD runs at 116.425 deg
    and is 0.0434" long.
  - The reference entry point is perpendicular to 116.44 deg - i.e. to VECTRIC'S OWN FIRST
    SAMPLED CHORD, not to the true tangent (a full degree away from it).

  - Our entry is perpendicular to OUR first sampled chord (115.90 deg, from the DXF's
    0.02" tessellation). Same policy, different sampling density.

So both programs place the entry perpendicular to the first chord of their own flattening
of the same true curve. Matching Vectric exactly would mean cloning its tessellator's step
selection, not fixing any geometric parameter. The offset DISTANCE is exact on both sides:
0.18996" against the 0.1900" tool radius of T5e - Amana 49706 Roundover 0.380.

1.8 thou, on an entry into air, with a 0.380" cutter.

NOW FIXED, previously listed here: arc segmentation (solved by arc provenance - see
ARC-FITTING.md; we emit 0 arcs there, matching the reference).

Everything else matches - audited pass by pass, not just through the harness's first-failure
report: all 4 T10 drills, all 6 T8 drills, the pocket, the T9 finishing pass (via the
crv3d-stored start point, as on board 3), and all cross-cuts on non-spline contours,
including sweep order and directions throughout.

See notes.md. Do NOT remove this marker to make the suite green.
