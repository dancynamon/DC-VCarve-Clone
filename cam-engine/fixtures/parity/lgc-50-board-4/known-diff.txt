Two narrow differences on the T5 op only:

1. Entry point 0.0018" out — ref (3.6712, 72.2940) vs ours (3.6720, 72.2924). The offset
   DISTANCE is right (0.1900", measured to +/-0.0001 over 78 mid-path samples); the
   residual is a ~0.0016" Y difference at the path ENDPOINT, where offsetOpenPath uses the
   first segment's normal and Vectric evidently does something slightly different. Sweeping
   the diameter does not move it, which is how we know it is endpoint treatment and not radius.

2. Arc segmentation — ref emits that path as pure G1, we fit 4 arcs to it.

Everything else matches: all 4 T10 drills, all 6 T8 drills (after de-duplicating the
DXF's coincident circles), the pocket, the T9 finishing pass, and ALL 7 CROSS-CUTS
including their sweep order and alternating directions.

See notes.md. Do NOT remove this marker to make the suite green.
