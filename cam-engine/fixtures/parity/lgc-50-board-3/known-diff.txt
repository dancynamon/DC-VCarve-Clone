ONE difference remains, on one pass, and it is a start-point rotation only.

  T9 finishing profile (the Ø1.5 circle at -0.5): the reference enters at 148.23 deg round
  the circle, we enter at 0 deg. Both cut exactly the same circle, r 0.68747, and the plunge
  is into already-cleared pocket, so it is in air either way. Everything about the pass
  matches except where it starts.

  Why 148.23 deg is not derivable from anything we can see. It is not the source contour's
  start (that is 0 deg, where we enter), not nearest to the previous pass's end (0 deg), not
  nearest to the park position (92.6 deg), and not the antipode (180 deg). Board 4's identical
  circle enters at 148.23 deg too, so it is deterministic - most likely just where Vectric's
  offsetter happens to begin its output polygon. Measuring the tessellation lattice supports
  that: the 100 vertices sit 0.63 deg off the source contour's own 0/90/180/270 vertices, so
  the offset result is not aligned to the input at all.

NOW FIXED, previously listed here:
  - all five cross-cut DIRECTIONS (the serpentine rule was wrong in principle; see
    camcore.js serpentineDirs)
  - the pocket entry point, ring radii, ring spacing and G1-vs-G2 representation
  - the first cross-cut's corner, which we mitred where Vectric fillets (5.9236" vs 5.8446")

Also correct: all 10 drill positions and their order, the T9 pass length, the xy envelope,
total cut length, feeds, spindle speeds and tool changes.

See notes.md and ARC-FITTING.md. Do NOT remove this marker to make the suite green.
